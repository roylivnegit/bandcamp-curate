# Nimble request usage (research POC)

Tracks real Nimble API spend against the 100-request cap Roy set on 2026-09-01, for the
"Second source: research first" backlog item **only**. This is the only backlog item
authorized to spend real Nimble credits — see `team/memory/backlog.md`.

**Before spending a single request**, read `TOTAL` below. If `TOTAL` plus what you're about to
do would exceed 100, stop and email Roy instead of proceeding — do not use your own judgment
to go over the cap.

**After every request** (whether it succeeded or not — a failed request still cost quota),
append a row to the log and update `TOTAL` in the same commit. Never lose a request off the
count because it happened, then the process died before this file got updated.

---

TOTAL: 0

## Log

| date | run (cycle-id or session) | requests this entry | running total | what for |
|---|---|---|---|---|
