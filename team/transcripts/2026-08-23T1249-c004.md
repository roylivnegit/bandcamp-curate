# Cycle 4 — 2026-08-23T1249-c004

Draining the five-hour usage window to 85%. Phases 1–7.


## 1 · Standup

```
842f072 feat(team): lower the usage-window ceiling from 98% to 85%
e002329 fix(deploy): stop overriding the worker's DATABASE_URL in compose
7f20528 feat(team): give the team its own claude login instead of guarding the shared one
7caa247 feat(team): only ever spend the personal account's quota
```

## 2 · Grooming

**Proposals**


**A feed nobody can read: put a floor and a weight on co-ownership**

- _title_: A feed nobody can read: put a floor and a weight on co-ownership
- _problem_: Roy opens the feed and gets ~1,600 records. Nobody scrolls 1,600 records, so in practice he sees the top of a list that was never really ranked — most of what's in it got there because a single stranger happened to own it.
- _proposal_: Two changes to the scorer, behind config so they can be tuned without a deploy. (1) A minimum co-owner count: an item owned by fewer than N neighbours is not a recommendation at all. (2) Weight each neighbour by how much their collection actually overlaps Roy's, divided by how big their collection is — so a 200-record collector who shares 40% of Roy's taste counts far more than an 8,000-record collector who co-owns everything with everyone. Ship both with unit tests over a synthetic corpus that contains one deliberate mega-collector and several tight-overlap collectors, and assert the mega-collector's picks fall below the tight ones.
- _why now_: This is the cheapest change with the largest effect on the only metric that matters, and it needs no new data source, no crawl, and no Nimble credits. Everything else on the backlog — tag coverage, explanations, a second source — improves a list that is currently unreadable. Fix the ordering before adding more to it.
- _surface_: curation
- _effort_: M
- _evidence_:
    - backend/app/curation/engine.py:386 — score = W_CO_OWNER * co_owners + W_TAG_AFFINITY * tag_affinity, no floor anywhere
    - backend/app/curation/engine.py:377 — the scoring loop iterates every grouped row, so co_owners == 1 produces a recommendation
    - backend/app/curation/engine.py:361 — co_owners is count(distinct FanItem.fan_id), unweighted by collection size
    - backend/app/curation/engine.py:45 — W_CO_OWNER = 1.0, W_TAG_AFFINITY = 0.25, hardcoded module constants
    - CLAUDE.md M4 Curation: 'curate → 1,600 recs (= 1,600 distinct bands)'
- _measure_: Feed size at each floor value (1/2/3/5) and the overlap between the old top 50 and the new top 50. If the top 50 barely moves, the weighting did nothing and only the floor mattered — that is a real finding, write it down. The real measure is Roy: how far down the list he still finds something worth clicking.
- _kill criteria_: Drop it if the co-ownership distribution turns out to be long-tailed in the wrong direction — i.e. almost every item already has 5+ co-owners, so a floor removes nothing and the flatness comes from somewhere else. Also drop the weighting half if neighbour collection sizes are all within the same order of magnitude, since then there is no mega-collector effect to correct.

**We cannot measure anything, because we have no database we are allowed to open**

- _title_: We cannot measure anything, because we have no database we are allowed to open
- _problem_: Every product question this team has — is the feed too long, is the ranking flat, does the tag signal exist — is a question about data. The only database with real data in it is production, which we are forbidden to touch. So today every product claim we make is a guess wearing a citation.
- _proposal_: Build the sandbox database and the seed corpus together, as one item (backlog E0-1 + E0-2). A local Postgres on its own port and its own compose project that refuses to start unless the database URL is local, migrated to head, loaded with a small synthetic corpus. The corpus is the product part: shape it deliberately so it has a mega-collector, several tight-overlap collectors, an item with exactly one co-owner, and at least one case of each exclusion path — owned, wishlisted, followed-by-host, liked, blacklisted. Then the curation engine can be run and its output looked at, by us, without spending a credit or reading a real user's collection.
- _why now_: It is the first item on the backlog for a reason, and the reason has not changed: the repo's own config points at Neon. Until this exists, the ranking work above is a change we ship blind and a reviewer approves on vibes.
- _surface_: infra
- _effort_: M
- _evidence_:
    - team/fixtures/ is empty — no seed.sql exists
    - team/tools/ contains only cycle.py, notify.py, run-cycle.sh, toml2schema.py, the plist — no sandbox-db.sh
    - CLAUDE.md M6: '.env DATABASE_URL now points at Neon direct' — the default config is production
    - backend/tests/fixtures/ holds three HTML files (album_page, fan_page, track_page) — the raw material to build the corpus from, without touching Neon
    - team/memory/metrics.md: 'Not collected automatically yet ... treat any claim about these as a guess'
- _measure_: The engine runs end to end against the sandbox and produces recommendations, and each of the five exclusion paths has a test that fails if that exclusion is removed. Secondary: the ranking item above becomes reviewable with real before/after numbers instead of an argument.
- _kill criteria_: Drop it if a corpus small enough to hand-write turns out to be too small to make the ranking visibly differ — if fifteen fans and forty albums cannot separate a good ranking from a bad one, then this is a test harness and not a measurement instrument, and we should say so and get the numbers a different way.

**Recommendations that arrive without a reason**

- _title_: Recommendations that arrive without a reason
- _problem_: A record Roy has never heard of, with no explanation attached, is one he does not click. The product already knows why it picked each item, but what reaches him is close to a bare number.
- _proposal_: Turn the reasons already stored on every recommendation into one readable sentence per row, computed at curation time and rendered in the feed: which of his own records led here, how many collectors who share his taste own it, and how tight that overlap is. The data is already written — co_owners, tag_affinity, matched_tags and seed_tags all land in reasons — it just is not composed into anything a person reads.
- _why now_: It is nearly free once the ranking item lands, because the weighted-overlap number that item computes is exactly the missing half of a trustworthy sentence ('7 collectors who share 40% of your taste'). Doing it before that change means writing the sentence twice.
- _surface_: curation
- _effort_: S
- _evidence_:
    - backend/app/curation/engine.py:394-400 — reasons dict already carries co_owners, tag_affinity, matched_tags, seed_tags
    - backend/app/curation/engine.py:235 — _seed_tag_provenance records which of your albums' supporters own the candidate
    - CLAUDE.md M4: seed-tag provenance is 'shown as via … in the UI' — a fragment, not a reason
    - charter.md 'What good looks like' #2: 'A reason he can read'
- _measure_: Every recommendation in the top 50 carries a non-empty sentence naming at least one of his own records or a genre. Then the real one: does he click more of them.
- _kill criteria_: Drop it if tag coverage is so thin that most sentences degrade to 'owned by 3 collectors' — that is not a reason, it is the score in words, and we should fix coverage first.


### Round 1

**[architect]** — _amend_

Proposal 1 is the right item, but its denominator is wrong and will invert the ranking. "How
big their collection is" does not exist in this schema — `Fan` has no item count and no
completeness flag (`backend/app/db/models.py:124-137`, only `last_crawled_at`). The only
thing you can divide by is `count(fan_items)` for that fan, and that number is crawl
progress, not collection size: a visit spends at most 10 pagination requests and parks the
rest (`backend/app/crawl/service.py:70`, CLAUDE.md "Pagination is bounded per visit and
resumable"). A collector we parked after 400 of their 8,000 items will look small, therefore
look tight, therefore get boosted — the exact opposite of the intended effect, and it will
look like a good result because the boosted fans are the ones we crawled least. Ship the
floor (config, default 1 = today's behaviour, so it is revertible by config not by deploy)
and ship overlap as a **numerator** — count of shared items with Roy — with no division,
until a fan-completeness marker exists. On proposal 2:
`backend/tests/test_curation.py:36-42` already stands up a full in-memory corpus via
`_build_graph`, and all five exclusion paths are already tested there — owned and wishlisted
at :114, blacklist at :172, followed-by-host at :185, liked-excludes-band at :233.
Rebuilding that in Postgres buys us almost nothing, and a synthetic corpus cannot answer the
question the proposal opens with. Its own kill criteria admit it: the distribution question
is about Roy's data, and hand-written fans cannot tell you whether real items already have
5+ co-owners. That is a read-only query against a database we are not allowed to open, which
makes it a `needs-roy` note, not a build item.

Evidence:
- backend/app/db/models.py:124-137 — Fan has no item_count and no completeness flag; only last_crawled_at
- backend/app/crawl/service.py:70 — PAGES_PER_VISIT = 10; a collection visit is bounded and parked, so fan_items count == crawl progress
- backend/app/curation/engine.py:355-367 — co_owners is a single GROUP BY count(distinct FanItem.fan_id); per-neighbour weights need a different query shape
- backend/tests/test_curation.py:36-42 — sqlite in-memory corpus harness already exists (_build_graph)
- backend/tests/test_curation.py:114,172,185,211,233 — owned+wishlist, blacklist, follow-by-host, seed-tag, liked-excludes-band each already have a test
- CLAUDE.md M4 — build_exclusions is the single place that decides Roy's world; a floor must sit after it, not replace it

Asks:
- Proposal 1 author: state which column you divide by. If the answer is count(fan_items), you are dividing by crawl completeness and I block in phase 3.
- Proposal 1: the floor must be config with default 1 (today's behaviour) and must apply after build_exclusions, not inside it — do not fork the exclusion path.
- Proposal 2 author: name one thing the Postgres sandbox lets us assert that backend/tests/test_curation.py:36 cannot. If the answer is 'JSONB/migrations run for real', say that — it is a smaller and more honest item.
- Someone write team/memory/needs-roy.md asking Roy for one read-only readout: histogram of co_owners per candidate, and fan_items count per neighbour. Both proposals' kill criteria depend on it and neither can obtain it.
- Proposal 3: hold until the sentence has a real second half. Today it can only say 'owned by N collectors', which its own kill criteria calls the score in words.

**[qa]** — _amend_

Agree with architect on numerator-not-ratio for proposal 1 — a division by fan_items count
would reward under-crawled fans, and I can't catch that with a test since it's a semantic
bug, not a crash. What I need on top of that fix: proposal 1's own test plan (mega-collector
ranks below tight collectors) is good but incomplete for the two ways this ships wrong
silently. First, a no-op regression test — default config (floor=1, no weighting change)
must reproduce today's exact recommendation set, so a bad default doesn't quietly reorder
Roy's feed on merge. Second, a test for the floor interacting with one_per_band dedup: if a
band's only candidate item falls below the floor, the whole band silently disappears from
the feed with no log line and no counter — that's exactly the class of failure CLAUDE.md
already got bitten by (followed-artist cap, frontier ownership). I want a
`filtered_by_floor` count somewhere in the curation run output, the same way exclusions are
already countable, or this is invisible if the floor is set wrong in prod.  On proposal 2:
architect's point stands and I'll go further — the exclusion-path coverage this proposal
wants already exists and is already good (test_curation.py:114,172,185,211,233, confirmed by
reading it just now). I don't need Postgres to test the curation engine; sqlite is
sufficient for that logic. Where a sandbox actually matters to me is JSONB-vs-JSON behavior
on `reasons`/`config`/`cursor` (backend/app/db/models.py:33, JSONVariant switches type by
dialect) and any future e2e gate — neither of which this proposal's corpus is built to
exercise, and frontend/e2e doesn't exist yet at all. So as scoped, proposal 2 buys us less
than it claims and I'd rather see the read-only Roy readout the architect asked for, plus
defer the sandbox build until something actually needs Postgres-specific behavior verified.

Evidence:
- backend/tests/test_curation.py:114,172,185,211,233 — exclusion paths already covered in sqlite
- backend/app/db/models.py:33 — JSONVariant = JSON().with_variant(JSONB, "postgresql"), the one real dialect-dependent behavior a sandbox would actually test
- frontend/e2e/ does not exist — confirmed via find, so proposal 2's corpus doesn't serve the E2E gate either
- CLAUDE.md M3 'Fan-out is also pruned by your follows' and the frontier-ownership incident — this codebase's recurring failure mode is silent exclusion/disappearance with no counter, which a co-owner floor reproduces if unguarded

Asks:
- Proposal 1: add a no-op regression test asserting default config (floor=1) reproduces the pre-change recommendation set exactly
- Proposal 1: emit a countable filtered_by_floor stat per curation run, not just a silently shorter list
- Proposal 2: rescope to 'answer the Roy readout' only, or name the specific Postgres-only behavior it needs to verify — otherwise I'd rather spend the slot on proposal 1

**[researcher]** — _amend_

Checked the "band_tags can carry signal for free" question from not_proposing — it's not
open, it's already answerable from the schema, no browser or credits needed. band_tags exist
and are explicitly "aggregated from a band's releases' album-page tags" (models.py:103-110),
meaning if we've crawled ANY release by a band, we have tag signal for every other release
by that band, including ones whose own album page was never fetched. But engine.py's tag
lookup only reads AlbumTag (_album_tags, line 212) — BandTag is never joined into candidate
scoring. That's free coverage sitting unused today, zero Nimble cost, and it bears directly
on proposal 3's kill criterion ("tag coverage so thin sentences degrade to 'owned by N'").
This changes the sequencing argument in proposal 3: it doesn't need to wait on the ranking
item to get a real second ingredient for the sentence — a band_tags fallback is available
right now, independently. Worth someone folding into whichever item touches engine.py's tag
lookup this cycle, rather than proposal 3 sitting idle behind proposal 1.  Separately, on
proposal 2: confirmed team/memory/research/ is genuinely empty (only .gitkeep) — that
evidence line is accurate, this isn't a research gap I can fill by reading fixtures. The
"read-only Roy readout" both architect and QA are asking for (co_owners histogram,
fan_items-per-neighbour) is Roy's local Neon data — outside what I can fetch even in
principle, since the charter bars me from touching production data same as everyone else.
That ask belongs in needs-roy.md, not a research task, and shouldn't block proposal 1 or 2
in this round — it's a parallel question, not a dependency.

Evidence:
- backend/app/db/models.py:103-110 — BandTag: "Tags a band carries (aggregated from its releases' album-page tags)"
- backend/app/curation/engine.py:212-225 — _album_tags queries only AlbumTag.album_id/tag_id, no BandTag join anywhere in engine.py (grep confirms 1 file, 0 BandTag references outside models.py)
- team/memory/research/ — ls -la shows only .gitkeep, 0 bytes, confirming the not_proposing claim
- charter.md Hard rules — 'Never touch production data... Never connect to Neon' applies to researcher same as any role, so the co_owners histogram ask cannot be a research deliverable

Asks:
- Whoever picks up proposal 3 (or a small addendum to proposal 1's engine.py touch): add a BandTag fallback in tag_affinity when an album's own AlbumTag rows are empty, and report how many currently-thin candidates gain a real tag as a result — that's a one-line measure of whether this free signal is worth anything before promising it in a sentence.
- File needs-roy.md for the co_owners/fan_items readout now regardless of which proposal wins — it's off both our critical paths and Roy can answer it whenever, no need to block grooming on it.


### Round 2

**[architect]** — _amend_

I was wrong in round 1 that no completeness marker exists: `mark_done` sets `cursor = None`
and a parked collection stays PENDING with a cursor (frontier.py:183-185, 199-201), so "this
fan's collection is fully paged" is a real, queryable fact — frontier.py:265 already queries
DONE globally across scans. That changes my objection from "never divide" to a condition: a
ratio is allowed only for fans with a DONE collection entry, and every other neighbour must
use the unweighted numerator, because for them `count(fan_items)` is still crawl progress.
My advice is still to ship the numerator alone this cycle and put the DONE-gated ratio in
the ADR as the next door, since mixing two weight scales in one score needs its own
experiment. Two mechanical constraints on whoever writes it: the score can no longer be a
single `count(distinct fan_id)` (engine.py:361) — per-neighbour weights need (item_id,
fan_id) pairs aggregated in Python, which is bounded by 22,606 fan_items total per
CLAUDE.md, so that is fine at this size; and QA is right about the floor, confirmed at
engine.py:440-441, where the sort and the `one_per_band` dedup both run after scoring, so a
floor applied before them removes the whole band, not one item. On the researcher's BandTag
find: I accept it, with a limit — BandTag is only written by `ingest_album`
(mapper.py:403,436), so it is a fallback for uncrawled releases of a crawled band, not new
coverage, and it must apply to the candidate side only. Feeding band tags into `tag_profile`
would silently widen `seed_tags`, and `exclude_seed_tag` is a user-facing filter whose
meaning would change with no migration and no note.

Evidence:
- backend/app/crawl/frontier.py:183-185 — mark_done sets status=DONE and cursor=None: fully paged, nothing left to resume
- backend/app/crawl/frontier.py:199-201 — mark_partial parks back to PENDING carrying a cursor, so PENDING+cursor == incomplete collection
- backend/app/crawl/frontier.py:265 — completed_elsewhere already queries status==DONE across scans; the completeness lookup exists
- backend/app/curation/engine.py:440-441 — scored.sort(...) then one_per_band dedup, both after the scoring loop, so a pre-dedup floor drops a band entirely
- backend/app/curation/engine.py:355-367 — album candidates are one GROUP BY count(distinct FanItem.fan_id); per-neighbour weighting needs a pair-level query instead
- backend/app/curation/engine.py:405-436 — tracks are a second, separate scoring loop with its own reasons dict; a floor added to only one of them forks the behaviour
- backend/app/bandcamp/mapper.py:403,436 — BandTag rows are written only inside ingest_album, i.e. only for bands with at least one crawled album page
- CLAUDE.md M6 — fan_items 22,606 rows total at migration time, the upper bound on a pair-level aggregation

Asks:
- Proposal 1: ship the shared-item numerator only. If you want the ratio in the same PR, gate it on the neighbour having a crawl_frontier collection entry with status DONE, and prove the gate with a test where a PENDING+cursor fan is NOT ratio-weighted.
- Proposal 1: apply the floor identically to the album loop (engine.py:375) and the track loop (engine.py:405), after build_exclusions and before sort/dedup, and count it once across both as QA's filtered_by_floor.
- Proposal 1: default floor = 1 and default weighting off, with QA's no-op regression test asserting byte-identical output to today. That is what makes this revertible by config instead of by deploy.
- BandTag fallback: candidate-side only. Add an assertion that seed_tags() / tag_profile are unchanged, so exclude_seed_tag keeps its current meaning.
- Proposal 2: I hold my round-1 position and QA has converged with it — rescope to the needs-roy readout. If someone still wants Postgres, the honest item is JSONVariant behaviour on reasons/cursor (models.py:33), which is small and is not this proposal.

**[qa]** — _amend_

Confirmed architect's mechanical read at engine.py:354-438: album and track candidates are
two separate query+loop pairs, each building its own `scored.append`, with no shared floor
point and no shared counter today. That's not a nitpick — it means "add a floor" is
naturally a two-line diff in two places, and it's exactly the shape that produces a fix that
works in tests (which likely only exercise albums, per test_curation.py) while silently
leaving tracks unfiltered. I'm converging with architect's constraint and tightening my own
ask: my no-op regression test and my filtered_by_floor counter both need cases that cover
the track loop (engine.py:419-438) independently, not just the album loop — otherwise a
reviewer sees green and ships a half-applied floor. On the DONE-gate: I'll write the test
architect is asking for (PENDING+cursor fan excluded from ratio weighting) as a named test,
e.g. `test_ratio_excludes_incomplete_neighbour`, so it's not left as a prose requirement
someone forgets. On proposal 2, I'm fully converged with architect now — down to naming
JSONVariant (models.py:33) as the one honest Postgres-only ask, which I already said in
round 1; nothing new to add, so I'll drop it from my round 3 ask unless someone reopens it.
On BandTag: I'll take the researcher's and architect's agreement and turn it into a concrete
gate — a test asserting `seed_tags()` output is byte-identical before/after the fallback
lands, since that's the one place silent scope-creep would actually reach a user-facing
filter (`exclude_seed_tag`).</argument> <parameter
name="evidence">["backend/app/curation/engine.py:354-401 — album loop: query, exclusion
check, scoring, append, all self-contained", "backend/app/curation/engine.py:403-438 — track
loop: separate query, separate exclusion check (no seed_tag/tag_affinity at all), separate
append — confirms architect's 'forks the behaviour' claim",
"backend/app/curation/engine.py:434 — track score is W_CO_OWNER * co_owners only, no
tag_affinity term, so a floor here has no tag interaction to worry about but still needs its
own dedup-ordering check"]

Asks:
- Whoever implements proposal 1: one shared floor-check helper called from both loops, one shared counter incremented in both, so the two loops can't drift
- Name the DONE-gate test test_ratio_excludes_incomplete_neighbour in the PR so it's traceable in review
- Add test_seed_tags_unchanged_by_band_tag_fallback (or equivalent) asserting exclude_seed_tag's candidate set is unaffected by the BandTag addition

**[researcher]** — _amend_

Verified architect's frontier claims — all check out, plus one gap worth flagging before
whoever builds the DONE-gate starts. `mark_done` sets status=DONE with no cursor
(frontier.py:183), and `completed_elsewhere` already queries status==DONE globally
(frontier.py:265), so the completeness fact is real and already-proven queryable. But
`CrawlFrontier` has no `fan_id` column at all — it's scoped by `(scan_id, url, kind)`
(models.py:333-347), not by fan. The join to "is this neighbour's collection DONE" has to go
through `Fan.url` (models.py:134, unique) matched against `CrawlFrontier.url`. That's a
real, working join — just a string match, not an FK, so whoever writes
`test_ratio_excludes_incomplete_neighbour` needs to construct frontier rows with `url ==
fan.url` and the right `kind`, not a `fan_id` filter. Small implementation note, not an
objection — I'm flagging it now so nobody discovers it mid-PR.  On BandTag: also verified —
`mapper.py:403` and `:436` are the only two writes, both inside `ingest_album`, confirming
architect's "fallback for uncrawled releases of a crawled band" framing exactly. Nothing to
add; converged.  On proposal 2: converged with both of you. My round-1 point stands
unchanged — the readout you're both asking for is Roy's production data and outside what I
can fetch even in principle, so it's a needs-roy.md item regardless of which proposal wins.
I'll write that note after grooming closes rather than block on it now.

Evidence:
- backend/app/crawl/frontier.py:183 — mark_done sets status=DONE, no cursor
- backend/app/crawl/frontier.py:265 — completed_elsewhere already queries CrawlFrontier.status == DONE
- backend/app/db/models.py:333-347 — CrawlFrontier has no fan_id column, unique on (scan_id, url, kind)
- backend/app/db/models.py:134 — Fan.url is unique String(512), the only join key available to a neighbour's frontier entry
- backend/app/bandcamp/mapper.py:403,436 — both BandTag writes are inside ingest_album, confirming the candidate-side-only framing

Asks:
- Whoever implements the DONE-gate: join CrawlFrontier.url == Fan.url (+ kind filter), there is no fan_id shortcut on CrawlFrontier
- Confirm test_ratio_excludes_incomplete_neighbour constructs frontier rows via url match, not a nonexistent fan_id column


### Round 3

**[architect]** — _amend_

One thing left that nobody has named, and it decides whether "behind config" is true or a
slogan: curate() has four call sites (feed.py:405, scan_service.py:362 and :430,
scripts/curate.py:34) and Settings has no curation section at all — the weights are module
constants at engine.py:45. If the floor arrives as a keyword argument threaded from callers,
three of those four keep the default while POST /api/recommendations/recompute uses whatever
the API passes, so the mid-crawl feed (scan_service.py:362, the crawl_curate_each_slice
path) gets ranked differently from the finalize pass over the same data — visible to Roy as
recs vanishing and reappearing during a scan, with nothing in the diff to explain it. Read
the floor and the weighting flag from Settings inside the engine, defaulted once; a kwarg
may exist as a test override only. Add that and the ADR is writable — everything else the
three of us asked for in rounds 1 and 2 is settled and I have no block left. On proposal 2 I
hold my round-1 position unchanged; QA and the researcher have both converged with it.

Evidence:
- backend/app/curation/engine.py:45 — W_CO_OWNER / W_TAG_AFFINITY are module constants, not settings
- backend/app/config.py — rg for curation|W_CO_OWNER|W_TAG_AFFINITY returns no matches; Settings has no curation section to add to
- backend/app/api/feed.py:405, backend/app/crawl/scan_service.py:362, backend/app/crawl/scan_service.py:430, backend/scripts/curate.py:34 — four independent curate() call sites, none sharing a config object
- CLAUDE.md 'The feed fills in DURING the crawl' — scan_service.py:362 re-curates every slice, so a per-call-site floor makes the running feed and the finalized feed disagree
- backend/app/curation/engine.py:300-306 — compute_recommendations signature is the single place a settings-derived default can land for all four callers

Asks:
- Proposal 1: floor and weighting-enabled come from Settings, read inside compute_recommendations, not threaded as kwargs by callers. A kwarg may exist only as a test override.
- Proposal 1: one test asserting all four curate() paths see the same floor value — or, cheaper, assert compute_recommendations picks up the setting with no argument passed.

**[qa]** — _support_

Satisfied. All three of my asks from rounds 1-2 — a shared floor-check helper called from
both the album and track loops with one shared filtered_by_floor counter, a named
test_ratio_excludes_incomplete_neighbour for the DONE-gate, and
test_seed_tags_unchanged_by_band_tag_fallback guarding exclude_seed_tag's meaning — are
reflected in the converged proposal. Architect's round-3 point (floor/weighting read from
Settings inside compute_recommendations, not threaded as kwargs by callers) closes the last
real gap: without it, the mid-crawl feed and the finalize pass could silently disagree,
which is exactly the class of invisible bug I can't catch with a green test suite. Proposal
1 as amended is ready for an ADR. Proposal 2 should be dropped in favor of a needs-roy.md
readout plus, if anyone wants Postgres specifically, a narrowly scoped JSONVariant-behavior
test — not this proposal's corpus, since sqlite already covers every exclusion path
(test_curation.py:114,172,185,211,233). Proposal 3 waits behind proposal 1 as scoped.

Evidence:
- backend/app/curation/engine.py:354-401 — album loop is self-contained (query, exclusion, score, append)
- backend/app/curation/engine.py:403-438 — track loop is a separate query+append with no tag_affinity term, confirming a floor needs one shared helper to avoid drift
- backend/tests/test_curation.py:114,172,185,211,233 — all five exclusion paths already covered in sqlite, no Postgres needed
- backend/app/db/models.py:33 — JSONVariant is the one honest Postgres-dialect-dependent behavior, and it is not what proposal 2 as scoped tests

Asks:
- Ship proposal 1 with: shared floor helper + shared filtered_by_floor counter across album and track loops; floor and weighting-enabled read from Settings inside compute_recommendations (kwarg only as test override); test_ratio_excludes_incomplete_neighbour joining CrawlFrontier.url == Fan.url; test_seed_tags_unchanged_by_band_tag_fallback; no-op regression test proving default config reproduces today's exact recommendation set
- Drop proposal 2 as scoped; file needs-roy.md for the co_owners/fan_items histogram instead
- Hold proposal 3 until proposal 1 lands

**[researcher]** — _support_

Satisfied with the converged shape of proposal 1. Nothing new to add this round — my two
contributions (BandTag fallback is candidate-side-only and free of Nimble cost;
CrawlFrontier has no fan_id, so the DONE-gate must join on Fan.url) are already folded into
architect's and QA's asks. Proposal 2 should drop as scoped in favor of needs-roy.md, which
I'll file after grooming closes since that readout is production data none of us can fetch.
Proposal 3 correctly waits behind proposal 1.

Evidence:
- backend/app/bandcamp/mapper.py:403,436 — both BandTag writes inside ingest_album, confirms candidate-side-only framing
- backend/app/db/models.py:333-347 — CrawlFrontier has no fan_id column, join must use Fan.url


### Ruling

**[lead] build**

- _decision_: build
- _chosen_: A feed nobody can read: put a floor and a weight on co-ownership
- _slug_: co-ownership-floor-and-weight
- _assignee_: backend-dev
- _why_: The feed is 1,600 items ranked by an unweighted count of distinct fans (engine.py:361), with no floor anywhere (engine.py:386), so a record owned by one stranger sits in the same list as one owned by seven collectors who share Roy's taste. That is the charter's first metric — precision over volume — and this is the only proposal that moves it without a new data source, a crawl, or a Nimble credit. Three rounds of grooming converged it into a shape with no block left: a shared floor helper across both the album and track loops, a countable filtered_by_floor stat so a misconfigured floor can't silently delete a band, and floor plus weighting read from Settings inside compute_recommendations so all four curate() call sites agree. I am deciding the two open defaults: floor ships at 1 because its correct value depends on a distribution only Roy can read, and the weighting ships on because a shared-item numerator with no division can only reorder the feed, never shrink it, which makes it the one bounded-risk change here that Roy actually sees this cycle.
- _scope limit_: In scope, and nothing else. (1) Two Settings fields — curation_min_co_owners (default 1) and curation_weighted_co_owners (default true) — read inside compute_recommendations, never threaded as kwargs by callers; a kwarg may exist as a test override only. (2) A shared floor-check helper called from both the album loop (engine.py:375) and the track loop (engine.py:405), applied after build_exclusions and before sort/dedup, with one shared filtered_by_floor counter incremented in both and surfaced in the curation run output. (3) Per-neighbour weighting as a shared-item COUNT NUMERATOR only — no division by anything, aggregated pair-level in Python (bounded by 22,606 fan_items). (4) Tests: no-op regression proving floor=1 + weighting off reproduces today's recommendation set exactly; mega-collector ranks below tight-overlap collectors; both assertions repeated independently for the track loop. EXPLICITLY OUT: the DONE-gated ratio and test_ratio_excludes_incomplete_neighbour (ADR next-door only, no CrawlFrontier/Fan.url join in this PR); the BandTag tag_affinity fallback and test_seed_tags_unchanged_by_band_tag_fallback; any change to tag_profile, seed_tags, or exclude_seed_tag semantics; any readable-sentence or frontend work; the Postgres sandbox; touching build_exclusions. If the pair-level aggregation turns out to need a query rewrite bigger than the two existing GROUP BYs, ship the floor alone and park the weighting — do not extend the cycle for it.
- _runner up_: Recommendations that arrive without a reason (proposal 3). It lost on sequencing, not merit — it is effort S and directly serves charter goal #2, but its own kill criterion is thin tag coverage, and the fix for that is the researcher's unused BandTag find (models.py:103-110, zero BandTag references in engine.py), which is a second engine.py change this cycle cannot absorb. It is first in line next cycle with the BandTag fallback folded in.
- _deferred_:
    - Recommendations that arrive without a reason — first in line next cycle, with the candidate-side-only BandTag tag_affinity fallback folded in, so the sentence has a real second half instead of the score in words.
    - We cannot measure anything, because we have no database we are allowed to open — dropped as scoped, not merely deferred. Architect, QA and researcher all independently confirmed test_curation.py:114,172,185,211,233 already covers every exclusion path in sqlite. The honest Postgres item is JSONVariant dialect behaviour (models.py:33) and that is a different, smaller proposal someone may raise on its own merits.
    - The DONE-gated ratio weighting — real and now proven queryable (frontier.py:183-185, 265), joined via CrawlFrontier.url == Fan.url since there is no fan_id column (models.py:333-347). Goes in this cycle's ADR as the next door. Mixing two weight scales in one score needs its own experiment.
    - needs-roy.md readout: co_owners histogram per candidate, fan_items count per neighbour. Researcher files it after grooming closes. It is off the critical path but it gates the floor's real default and both of this proposal's kill criteria.
    - Second source research (Beatport / Discogs / RA / SoundCloud) — memory/research/ is empty apart from .gitkeep. Not this cycle's build slot; the researcher may start the note in parallel if the window allows.


## 3 · Design

**[architect] ADR-0002**

- _title_: ADR-0002 — A floor under a rec, and a weight on the stranger who made it
- _problem_: engine.py:361 scores an item by count(distinct FanItem.fan_id) over the scan's neighbours and engine.py:386 turns that straight into a score with no minimum anywhere. One stranger owning a record makes it a recommendation, and every stranger counts the same — the 8,000-record collector who co-owns everything with everyone counts exactly as much as the 200-record collector who shares half of Roy's taste. The result is ~1,600 recs (CLAUDE.md, M4) ordered by something close to noise. Note: ADR-0001 (cycle c003) ruled build on the floor alone and never landed — config.py has no curation section, engine.py:300-308 has no floor param, no branch carries it. This ADR supersedes it.
- _approach_: Both changes live inside compute_recommendations (engine.py:300); nothing above it changes. Two Settings fields — curation_min_co_owners (default 1) and curation_weighted_co_owners (default true) — are read from get_settings() inside the engine, never threaded from the four curate() call sites (feed.py:405, scan_service.py:362, scan_service.py:430, scripts/curate.py:34), because :362 is the mid-crawl re-curate and :430 is finalize; a caller-supplied value would make the running feed and the finished feed rank the same data differently. Kwargs exist only as a test override (test_curation.py:44 already forwards **kw). The floor is one helper called from the album loop (engine.py:377) and the track loop (engine.py:419), after the exclusion and exclude_seed_tags checks and before scored.sort / one_per_band (engine.py:440-450), incrementing one shared per-call counter. The weight is a count, never a ratio: each co-owner contributes 1 + overlap(f), where overlap(f) is how many of Roy's owned items that fan also owns. No division — the only available denominator, count(fan_items), is crawl progress (crawl/service.py:70 parks collections mid-pagination; models.py:124-137 has no item count), so a ratio boosts the fans we crawled least. The numerator is crawl-contaminated too, but in the safe direction: under-crawled means under-weighted, never inflated. This turns the two GROUP BYs into pair-level (item_id, fan_id) selects aggregated in Python, bounded by fan_items (22,606 rows, CLAUDE.md M6); per the Lead, if that rewrite grows past the two existing GROUP BYs, ship the floor alone and park the weight. stats_out: dict | None on compute_recommendations and curate carries min_co_owners, weighted, candidates, filtered_by_floor into scan.stats at scan_service.py:366 and :437, which api/scans.py:43,68 already passes through as an open dict — per call, never module state, since slices drain at crawl_concurrency 50. reasons["co_owners"] keeps meaning distinct fans; the weighted figure is a new key, co_owner_weight.
- _invariants_:
    - build_exclusions is untouched — no diff in it, no second exclusion path. The floor is a threshold, not an exclusion.
    - Floor and weight are applied after the exclusion checks (engine.py:378, engine.py:420) and after exclude_seed_tags, never before — so filtered_by_floor never counts an item already excluded as owned / wishlisted / followed-by-band-id / followed-by-host / liked / blacklisted.
    - Default floor (1) with weighting OFF reproduces today's feed exactly: same ids, same order, same scores, on the existing fixture graph.
    - Weighting ON at floor 1 changes order only, never membership — the id set is identical to the parity run. Asserted, not assumed; this is the Lead's stated reason for defaulting it on.
    - One helper, one counter, both loops: the album loop and the track loop call the same floor function and increment the same counter. No `if co_owners < floor` written twice, no filtering in store_recommendations or the API layer.
    - Album and track scores stay on one scale — both read the same weight map. one_per_band (engine.py:441) compares an album against a track of the same band, so weighting one loop and not the other silently flips which release survives.
    - The floor tests the raw distinct-neighbour count, not the score: `co_owners >= floor`, never `score >= floor`.
    - The weight has no denominator — no division, no len(fan_items), no ratio anywhere in the weight expression. Every co-owner weighs >= 1, so weighting can never drop a candidate to zero.
    - Overlap is computed against Roy's owned items only (is_wishlist false) and never includes Roy himself (fan_id != me.id, already at engine.py:334,341).
    - Config is read at one edge: `grep -rn curation_min_co_owners` returns app/config.py and app/curation/engine.py only — not feed.py, not scan_service.py, not scripts/curate.py.
    - No shared mutable state — the counter and stats_out are per-call locals. No module-level or class-level accumulator (two scans curate concurrently).
    - No schema change, no migration, no data altered: alembic/versions/ gains no file; scan.stats gains keys.
    - Nothing in the diff can spend a credit: no app/scraping/ call, no scan start, no worker drain.
    - Any existing assertion in test_curation.py that moves (e.g. A4 > A5 under weighting) is named and justified in the PR body. A quietly rewritten expectation is how a wrong ranking ships green.
- _changes_:
    - backend/app/config.py — add curation_min_co_owners: int = 1 and curation_weighted_co_owners: bool = True, each with a comment saying why
    - backend/app/curation/engine.py — read settings in compute_recommendations; pair-level candidate queries replacing the two GROUP BYs (engine.py:355-368, :404-417); per-neighbour overlap map; shared floor helper + shared counter; co_owner_weight in reasons; stats_out on compute_recommendations and curate
    - backend/app/crawl/scan_service.py:366 and :437 — pass stats_out and merge it into scan.stats
    - backend/tests/test_curation.py — the seven tests below, on the existing sqlite in-memory fixture
    - Tables: none. Migrations: none.
    - Endpoints: none — GET /api/scans already passes scan.stats through as an open dict (api/scans.py:43,68)
- _tests_:
    - Parity: floor 1, weighting off → identical ids, order and scores to today (backend/tests/test_curation.py)
    - Weighting reorders, never shrinks: same graph with weighting on → identical id set to the parity run
    - Mega-collector loses: a fan owning 200 items but only 2 of Roy's, versus two fans owning 30 items with 15 of Roy's; an item owned only by the mega-collector ranks below an item owned only by a tight one. The corpus must make 'mega' mean large collection AND small absolute overlap — otherwise the test proves nothing
    - The mega-collector assertion repeated independently on a track candidate — the track loop has no tag term (engine.py:434) and is the one most likely to be left behind
    - Floor cuts both loops: a band whose only items are a 1-co-owner album and a 1-co-owner track produces zero recs at floor 2
    - Counter is honest: filtered_by_floor equals the number cut and does not count an item build_exclusions had already removed
    - Settings reach the engine with no kwargs: patched settings change the result of a bare compute_recommendations(...) call — this is what makes all four curate() call sites agree
- _rollback_: Single `git revert` of the PR. No migration and nothing persisted that survives it: store_recommendations clears and re-inserts a scan's recs wholesale (engine.py:458-472), so the next curate restores the old feed, and leftover scan.stats keys are inert JSON. Before a revert is needed, setting CURATION_WEIGHTED_CO_OWNERS=false (with floor 1) restores today's ranking at runtime with no deploy — that is the point of reading both from Settings.
- _risks_:
    - SILENT: the numerator does not fix every mega-collector. It demotes the fan with a big collection and small absolute overlap; a fan with a big collection and big overlap still wins. If the test corpus conflates the two, the suite goes green while proving less than it claims. The real fix is the DONE-gated ratio.
    - A floor above 1 can empty a thin feed — a custom scan had 17 neighbours live (CLAUDE.md, M3). ADR-0001's min_neighbours_for_floor gate is out of scope, so filtered_by_floor in scan.stats is the only thing separating 'high floor' from 'no data'.
    - Weighting ships on by default and changes Roy's ranking with no before/after captured, because we cannot read production. The comparison has to come from him.
    - Mid-crawl drift: crawl_curate_each_slice re-curates while neighbours and overlaps grow, so the order moves between two polls. Expected; stats is how a reader tells that from a bug.
    - Memory: pair-level aggregation holds a fan-id set per candidate in Python. Fine at 22,606 fan_items; fails as a worker OOM, not as a wrong answer, if the graph grows orders of magnitude.
- _rejected_:
    - overlap ÷ collection size — the only available denominator is count(fan_items), which is crawl progress (crawl/service.py:70; models.py:124-137 has no item count). It boosts the fans we crawled least, and does it in a way that looks like a good result.
    - The DONE-gated ratio — real and now known to be queryable: mark_done clears the cursor (frontier.py:183-185), a parked collection stays PENDING with one (:199-201), and completed_elsewhere (:265) already selects on DONE across scans; the join is CrawlFrontier.url == Fan.url since CrawlFrontier has no fan_id (models.py:333-347). Deferred because mixing two weight scales in one score needs its own experiment. This is the next door.
    - The floor as SQL HAVING (ADR-0001's approach) — it counts before exclusions, so filtered_by_floor would not mean what a reader thinks; and the weighted path needs pair-level rows anyway, so the HAVING saves nothing.
    - The floor after one_per_band — a below-floor item would keep its band alive and suppress the band's better release, and the counter would stop meaning anything.
    - The floor on the weighted score — unreadable (nobody can look at a rec and say what cleared it) and it would not match the co_owners histogram Roy is being asked for.
    - Threading floor/weight as kwargs from the four curate() call sites — the mid-crawl feed (scan_service.py:362) and finalize (:430) would then rank the same rows differently, visible to Roy as recs vanishing and reappearing during a scan.
    - Overwriting reasons['co_owners'] with the weighted number — it is the readable half of next cycle's sentence and the unit of Roy's histogram. Add a key; do not redefine one.
    - ADR-0001's min_neighbours_for_floor gate and bands_cut_no_multi_co_owner diagnostic — both good, both out of this scope, and both inert while the floor default is 1.
    - A log line instead of a counter in scan.stats — not queryable after the run, which is the only time anyone asks.
    - The Postgres sandbox — test_curation.py:114,172,185,211,233 already covers all five exclusion paths in sqlite.

**[product]** — _amend_

It solves the ranking half and ships the shrinking half as a dial nobody can turn. My
problem statement was "Roy opens the feed and gets ~1,600 records"; with floor defaulting to
1 the merged artifact leaves it at 1,600. I accept that default — the right floor depends on
a distribution only Roy can read — but then the deliverable has to include the thing that
lets him read it, and right now it does not. `filtered_by_floor` lands in `scan.stats`,
which the frontend types as `{recommendations?, credits?}` (frontend/src/api/types.ts:72)
and renders only credits (ScanListPage.tsx:178), so the counter is invisible everywhere Roy
actually looks. The one place he can run this for free is `scripts/curate.py`, which he
already runs locally against his own DB, and which today prints just a count and the top N
(scripts/curate.py:34-35). Add `stats_out` printing there — floor, candidates,
filtered_by_floor — and `CURATION_MIN_CO_OWNERS=3 python -m scripts.curate 25` becomes a
real, zero-credit, zero-deploy experiment he can run three times in five minutes and tell us
which floor to default to next cycle. That is three lines, touches no invariant in the ADR,
and it is the difference between this cycle producing a ranking change and producing a
ranking change plus the measurement that unblocks the needs-roy readout everyone is waiting
on. Second, smaller: name in the ADR's own text, not just its risk list, that a big-
collection/big-overlap collector is still top-weighted — that fan is the omnivore, and he is
the one Roy will notice surviving.

Evidence:
- frontend/src/api/types.ts:72 — stats typed as { recommendations?: number; credits?: number }; filtered_by_floor passes through JSON but nothing reads it
- frontend/src/features/scans/ScanListPage.tsx:178-181 — the only scan.stats key rendered anywhere in the UI is credits
- backend/scripts/curate.py:34-35 — curate() then print(f"computed {len(scored)} recommendations"); no stats surface, and this is the one free local path
- backend/app/curation/engine.py:45 — W_CO_OWNER = 1.0 / W_TAG_AFFINITY = 0.25 confirm the scorer is flat today, as the ADR states
- CLAUDE.md 'Going-forward workflow' — Roy crawls locally with scripts, so a local env var + scripts.curate needs no Render redeploy
- render.yaml:33-42 — hosted API config is dashboard env vars, so changing the floor there is a restart, not a knob he turns while looking at the feed

Asks:
- Add stats_out printing to scripts/curate.py — floor, candidates, filtered_by_floor — so CURATION_MIN_CO_OWNERS=N python -m scripts.curate is a runnable experiment. In scope, no invariant touched, ~3 lines.
- Put the floor-sweep instruction in the PR body as a numbered thing for Roy to run, with the three values to try (2/3/5) and what to report back — that is the input the needs-roy readout was asking for anyway.
- State in the ADR body, not only its risks, that a large-collection large-overlap neighbour is still top-weighted; the numerator demotes the sparse omnivore, not the dense one.


## 4 · Build

_backend-dev hit its per-turn cap and was cut short._

**[backend-dev]**



```
backend/app/config.py             |  10 ++
 backend/app/crawl/scan_service.py |   9 +-
 backend/app/curation/engine.py    | 184 ++++++++++++++++++++++++++++-----
 backend/tests/test_curation.py    | 208 ++++++++++++++++++++++++++++++++++++++
 4 files changed, 381 insertions(+), 30 deletions(-)
```


## 5 · Review

_Cycle ended early: the usage window is exhausted (status 'rejected'). Work is parked on `team/2026-08-23T1249-c004-co-ownership-floor-and-weight`._

